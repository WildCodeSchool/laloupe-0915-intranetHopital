{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Charte_utilisateur</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Signature</th>
                <td>{{ entity.signature }}</td>
            </tr>
            <tr>
                <th>Datesignature</th>
                <td>{{ entity.dateSignature|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('charte') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('charte_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
