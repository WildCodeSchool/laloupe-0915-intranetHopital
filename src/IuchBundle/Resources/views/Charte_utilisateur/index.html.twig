{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Charte_utilisateur list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Signature</th>
                <th>Datesignature</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('charte_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.signature }}</td>
                <td>{% if entity.dateSignature %}{{ entity.dateSignature|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('charte_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('charte_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('charte_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
