{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Charte_utilisateur creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('charte') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
