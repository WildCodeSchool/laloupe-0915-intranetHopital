{% extends '::base.html.twig' %}

{% block body %}
    <object width="860" height="700" data="{{ asset('bundles/iuch') }}{{ vich_uploader_asset(charte, 'fichier') }}" type="application/pdf"></object>

    {{ form(form) }}
{% endblock %}